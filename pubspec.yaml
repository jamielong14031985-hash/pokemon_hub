name: pokemon_hub_flutter
description: Pokemon Hub Flutter app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  image_picker: ^1.2.1
  google_mlkit_text_recognition: ^0.15.1
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  path_provider: ^2.1.5
  camera: ^0.12.0+1
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
